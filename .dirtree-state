# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
annotate=[
	LEARNINGS.md = Hard-won toolchain/codebase lessons (sqlite-vec UBSan trap, Debug-hides-release-bug)
	build.zig = Build: links dep sqlite3/sqlite_vec0 with sanitize_c=.off (vec0 UBSan-traps under -O2); wires unit tests
	test = Full test runner: nix build + zig unit/integration tests + bash CLI suite; exit code = failing groups
	tests/cli/fixtures/conversations = Committed conversation fixture (alpha/beta/gamma projects) used to build a deterministic index for CLI tests
	tests/cli/search_cli_test.sh = CLI integration tests: default scoping, --all, --project partial path/name, env overrides, verbose messaging over a fixture index
]
